# mongoose-fuzzy-searching@2.0.2 declares a mongoose 5.x peer dep but works
# with the mongoose 6 used here; without this flag npm 7+ (including Vercel's
# build) refuses to install
legacy-peer-deps=true
